package:
  name: pyepics
  version: "3.2.5rc2"

source:
  fn: pyepics-3.2.5rc2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyepics/pyepics-3.2.5rc2.tar.gz
  md5: d378eda122ee65ef53fe4c8cedc4386d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyepics = pyepics:main
    #
    # Would create an entry point called pyepics that calls pyepics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - numpy
    - pyparsing

test:
  # Python imports
  imports:
    - epics
    - epics.autosave
    - epics.compat
    - epics.devices

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyepics.github.io/pyepics/
  license: Epics Open License
  summary: 'Epics Channel Access for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
